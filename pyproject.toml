[project]
name = "english-test-generator"
version = "0.1.0"
description = "A Streamlit app to generate English tests and export to PDF."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "streamlit>=1.39.0",
    "fpdf2>=2.7.4",
    "requests>=2.31.0",
]
